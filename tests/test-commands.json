{
  "commands": [
    {
      "command": "ls",
      "parameters": ["-la"],
      "description": "list files with details",
      "nameAlias": "ls -la"
    },
    {
      "command": "cat",
      "parameters": ["/etc/hosts"],
      "description": "describe the file /etc/hosts",
      "nameAlias": "cat hosts"
    },
    {
      "command": "sleep",
      "parameters": ["3s"],
      "description": "sleep for 3 seconds",
      "nameAlias": "sleep 3s"
    },
    {
      "command": "curl",
      "parameters": ["--verbose","google.com"],
      "description": "get google\"s web page in verbose mode",
      "nameAlias": "curl google"
    },
    {
      "command": "yay",
      "parameters": ["-Ss","pulseaudio"],
      "description": "for some reason search for pulseaudio at aur",
      "nameAlias": "yay search pulseaudio"
    },
    {
      "command": "tree",
      "parameters": ["/home/thassiov/Knowledge"],
      "description": "get the tree strucure of the Knowledge directory",
      "nameAlias": "tree Knowledge"
    }
  ]
}
